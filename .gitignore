# Reps' personal data must never be committed to this public repo.
PROFILE.md
find-leads-log.md

# OS junk
.DS_Store

# Never public: real Bliss QuickBase config (realm/app/table/field IDs, live record counts).
# This entire folder must never be pushed to the public repo, by name or by pattern.
private/
*.skill

# Internal build-process notes, not something reps need to see.
HANDOFF.md
